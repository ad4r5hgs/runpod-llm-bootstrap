# Proven configuration from the experimental A40 run.
# Copy to config.env and edit as needed.

# llama.cpp release tested successfully in the RunPod A40/CUDA 12.8 environment.
LLAMA_CPP_TAG="b10603"
LLAMA_CPP_EXPECTED_COMMIT="c060ca974"
LLAMA_CPP_CUDA="12.8"

# Hugging Face repository and exact files that were tested.
MODEL_REPO="philbert440/Qwen3.8-27B-Uncensored-Aggressive-GGUF"
MODEL_REVISION="main"
MODEL_FILE="Qwen3.8-27B-Uncensored-Balanced-Q8_0.gguf"
MTP_FILE="mtp-Qwen3.8-27B-Uncensored-Balanced-Q8_0.gguf"

# Runtime defaults. 65536 is the last context size explicitly verified in our run.
# Change to 131072 only after confirming 128K works reliably on your A40 setup.
CONTEXT_SIZE="65536"
GPU_LAYERS="999"
MTP_GPU_LAYERS="999"
MTP_DRAFT_N_MAX="4"
REASONING_EFFORT="medium"

# Storage / paths.
INSTALL_DIR="/opt/qwen-runpod"
MODEL_DIR="/models"
LLAMA_DIR="${INSTALL_DIR}/llama.cpp"

# Hugging Face download parallelism.
HF_MAX_WORKERS="8"

# Server defaults. Keep localhost until remote access is explicitly configured.
SERVER_HOST="127.0.0.1"
SERVER_PORT="8080"
SERVER_MODEL_ID="qwen3.8-27b-philbert"
# Set this only when using the API server. Do not commit secrets.
LLAMA_API_KEY=""

# Optional: download the vision projector for multimodal/image inference.
DOWNLOAD_VISION_PROJECTOR="false"
VISION_FILE="mmproj-Qwen3.8-27B-Uncensored-Balanced-F16.gguf"
